import { UniversalAppInterceptor } from './universal-app-interceptor';

describe('UniversalAppInterceptor', () => {
  it('should create an instance', () => {
    expect(new UniversalAppInterceptor()).toBeTruthy();
  });
});
